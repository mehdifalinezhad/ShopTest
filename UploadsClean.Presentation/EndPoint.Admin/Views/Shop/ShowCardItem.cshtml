@model UploadsClean.Common.Dto.CardItemDto

<div class="card">
    <div class="card-body">
        <h6 class="card-title">سبد خرید شما </h6>
        <form role="form" method="post" asp-controller="Shop" asp-action="BuyProduct" class="needs-validation" novalidate="" enctype="multipart/form-data">
            <input type="hidden" asp-for="Id">
        
            <div class="table-responsive">
                <table class="table">
                    <thead>
                        <tr>
                            <th scope="col">ردیف</th>
                            <th scope="col">نام</th>
                            <th scope="col">عکس</th>

                            <th scope="col">ویرایش</th>
                            <th scope="col">حذف</th>
                            @* <th scope="col">افزودن به سبد خرید</th> *@
                        </tr>
                    </thead>
                    <tbody>

                        @{
                            int Count = 0;
                        }
                       @*  @foreach (var item in Model)
                        {
                            <input type="hidden" asp-for="@item.Id"/>
                            Count = Count + 1;
                            <tr>
                                <td scope="row">@Count</td>
                                <td>@item.Name</td>
                                <td><img src="@item.ImageUrl" style="height:30px;width:50px;" /></td>
                                <td><a href="/product/GetproductById?Id=@item.Id">ویرایش</a></td>
                                <td><a href="/Category/CategoryDel?Id=@item.Id"> del</a></td>
                                @*  <td><a href="/Shop/AddToCart?Id=@item.Id"> +</a></td> 
                            </tr>

                        } *@
                    </tbody>
                </table>
            </div>
    
            <div class="row">
                <button class="btn btn-primary" type="submit">ویرایش </button>
                <div class="col-md-3 mb-3">
                    <label>تعداد خرید </label>
                    <input type="number" id="countBuy" class="form-control">

                </div>
                <button class="btn btn-Success" onclick="AddTOCart()">افزودن به سبد خرید </button>
            </div>
        </form>
    </div>

</div>